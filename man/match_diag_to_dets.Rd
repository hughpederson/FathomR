% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_diag_to_dets.r
\name{match_diag_to_dets}
\alias{match_diag_to_dets}
\title{Match Diagnostic Data to Detections}
\usage{
match_diag_to_dets(
  dets,
  diag,
  diag_sources = NULL,
  time_var = "Time",
  serial_var = "Serial",
  diag_vars = NULL,
  keep_diag_time = FALSE,
  diag_time_col = "diag_time",
  suffix = "_diag",
  max_diff = NULL,
  tie = c("earlier", "later")
)
}
\arguments{
\item{dets}{A data frame of detections containing `time_var` and `serial_var`.}

\item{diag}{A data frame of diagnostic records, or a list of data frames.}

\item{diag_sources}{When `diag` is a list, either a character vector of list
names to include or a list of data frames to bind together.}

\item{time_var}{Name of the time column present in both `dets` and `diag`.}

\item{serial_var}{Name of the serial/receiver column present in both `dets`
and `diag`.}

\item{diag_vars}{Character vector of diagnostic columns to append. Defaults
to all columns in `diag` except `serial_var` and `time_var`.}

\item{keep_diag_time}{Logical; if `TRUE`, include the matched diagnostic time
in the output.}

\item{diag_time_col}{Name of the output column to store matched diagnostic
time when `keep_diag_time = TRUE`.}

\item{suffix}{Suffix appended to diagnostic column names if they collide with
columns in `dets`.}

\item{max_diff}{Optional numeric maximum allowed absolute time difference.
If `time_var` is POSIXct, this is interpreted as seconds.}

\item{tie}{Tie-breaking rule when distances are equal: `"earlier"` or
`"later"`.}
}
\value{
A data frame with the same rows as `dets`, augmented with the matched
diagnostic columns (and optionally the matched diagnostic time).
}
\description{
Match each detection row to the nearest diagnostic record (by time) within
the same serial, and append selected diagnostic fields to the detections.
}
\examples{
\dontrun{
dets <- data.frame(
  Serial = c("A", "A", "B"),
  Time = as.POSIXct(
    c("2020-01-01 00:00:05", "2020-01-01 00:00:15", "2020-01-01 00:00:10"),
    tz = "UTC"
  )
)
diag <- data.frame(
  Serial = c("A", "A", "B"),
  Time = as.POSIXct(
    c("2020-01-01 00:00:00", "2020-01-01 00:00:20", "2020-01-01 00:00:12"),
    tz = "UTC"
  ),
  Temp = c(1, 2, 3)
)
match_diag_to_dets(dets, diag, diag_vars = "Temp")
}
}
